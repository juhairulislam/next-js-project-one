import { montserrat } from '@/app/layout';
import React from 'react';

const LoginPage = () => {
    return (
        <div className={`${montserrat.className}`}>
            <h1>THis is login page re bhai</h1>
        </div>
    );
};

export default LoginPage;